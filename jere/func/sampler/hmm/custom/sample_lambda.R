sample_lambda <- 
  function(
    y, log_lik_func, 
    z, m, x,
    p0, pk,
    theta, lambda,
    hyper
    )
    # to be completed
  {
    gamma <- hyper$gamma
    alpha <- hyper$alpha
    kappa <- hyper$kappa
    
    lambda
  }