sample_hmm_lambda <- 
  function(
    # data & likelihood
    x, log_lik_func, 
    # CRF parameter, prior
    gamma, alpha, kappa, lambda,
    # CRF parameter, data
    z, m, p0, pk, n_jk)
  {
    lambda
  }